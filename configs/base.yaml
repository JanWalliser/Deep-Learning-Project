seed: 42
device: auto

data:
  root: data/raw
  num_workers: 2
  batch_size: 128
  val_split: 0.1

training:
  epochs: 2
  lr: 1e-3
  weight_decay: 0.0

logging:
  use_wandb: true
  project: dl-class-imbalance-mnist
  entity: null
  run_name: null

model:
  name: fc
  num_classes: 10
